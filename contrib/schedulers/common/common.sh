# Path for executables (OMPI, UCX and OSU)
export BUILD_PATH=.

# Maximal number of processes per node (typically equal to the number of cores per node)
export MAX_PPN=24
# Note: SLURM scripts override this setting

# Optional extensions
# ===================

# module load gcc
# export CC=`which gcc`
# export CXX=`which g++`
